<Window x:Class="ShapeEditor.CustomShapeEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:behaviors="clr-namespace:ShapeEditor.Behaviors"
        xmlns:local="clr-namespace:ShapeEditor"
        mc:Ignorable="d"
        Title="Create Custom Shape" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Canvas Grid.Row="0" Background="WhiteSmoke">
            <!-- Здесь будет рисоваться наша фигура -->
            <Path Stroke="Blue" StrokeThickness="2" Data="{Binding PathGeometry}"/>
            
            <!-- Прикрепляем поведение для рисования -->
            <b:Interaction.Behaviors>
                <behaviors:DrawingBehavior
                    StartDrawingCommand="{Binding StartDrawingCommand}"
                    DrawingCommand="{Binding DrawingCommand}"
                    EndDrawingCommand="{Binding EndDrawingCommand}"/>
            </b:Interaction.Behaviors>
        </Canvas>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="Save" Width="75" Margin="5" Click="SaveButton_Click"/>
            <Button Content="Cancel" Width="75" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
